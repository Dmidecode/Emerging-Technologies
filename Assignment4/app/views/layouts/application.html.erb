<!DOCTYPE html>
<html>

<head>
  <title><%= title %></title>
      <%= render 'layouts/stylesheets' %>
	 <%= csrf_meta_tags %>
</head>

<body>
<!-- Header apart of the main div on the left -->
 <!-- <%= render 'layouts/leftMenu' %> -->

<div class="container">
    <%= render 'layouts/header' %>

  <section class="round">
        <% flash.each do |key, value| %>
          <div class="flash <%= key %>"><%= value %></div>
        <% end %>
        <%= yield %>
  </section>

  <%= render 'layouts/footer' %>


  <!-- REMOVE TO REMOVE DEBUG -->
  <%= debug(params) if Rails.env.development? %>

</div>


<!-- footer apart of the main div on the right-->
 <!-- <%= render 'layouts/rightMenu' %> -->

</body>

</html>
